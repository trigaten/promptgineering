export const metadata = {
  sidebar_position: 40,
  title: "🟢 كتابة الأكواد منطقيًّا",
};

# كتابة الأكواد منطقيًّا

تشكّل نماذج اللغات بمساعدة البرامج (PALs) مثالًا لنظام MRKL. إذ أنّه عند سؤالك لهذه النماذج ستكون قادرةً على كتابة شيفرات برمجية ومن ثمّ حلّ السؤال. تقوم النماذج بإرسال الشيفرة البرمجية إلى التنفيذ للحصول على النتائج. يعمل نموذج اللغات بمساعدة البرامج بشكلٍ مغاير لتعليمات تسلسل الأفكار. فالمنطق الوسيط لـ PAL هو الشيفرة، في حين أن تفكير تسلسل الأفكار هو لغة طبيعية.
![PAL Example](https://learnprompting.org/assets/images/pal-08b7777800187b41a639d2a8129e7756.webp)

من المهمّ أن تعرف أنّ نماذج اللغات بمساعدة البرامج تفصل بين اللغات الطبيعية والشيفرات البرمجيّة. في الصورة السابقة، الجمل التي تمّ تظليلها بالأزرق تدلّ على تفكيرٍ منطقيّ باللغات الطبيعية تمّ توليده بواسطة النموذج. ولكنّ ما لا يوجد في الصورة هو أن نموذج اللغات بمساعدة البرامج يقوم بتوليد "#" قبل كلّ سطر من التفكير المنطقي باللغات الطبيعية، بحيث يتم تفسيرها على أنها تعليقاتٌ عند التنفيذ للشيفرات البرمجيّة.

## مثال
لننظر إلى هذا المثال الذي يظهرّ حلّ نموذج اللغات بمساعدة البرامج لسؤالٍ رياضيّ. أنا اعتمدت على تعليمةٍ بثلاثة أمثلة، والتي تعتبر نسخة مبسطةً من [هذه التعليمة](https://github.com/reasoning-machines/pal/blob/main/pal/prompt/math_prompts.py).

سأستخدم langchain، وحزمة Python لتسلسل وظائف نموذج اللغات الكبيرة لهذا الغرض. أولاً، نحتاج إلى تنصيب بعض التعليمات:

```
!pip install langchain==0.0.26
!pip install openai
from langchain.llms import OpenAI
import os
os.environ["OPENAI_API_KEY"] = "sk-YOUR_KEY_HERE"
```
ثمّ يمكننا إنشاء مثيل لـ GPT-3 davinci-002 (يحدث استدعاء API عندما نستخدم هذا الأمر).
```
llm = OpenAI(model_name='text-davinci-002', temperature=0)
```
هذه تعليمةٌ متعدّدة الأمثلة:
```
سؤال: هناك تسعة حواسيب في غرفة الخادم. تمّ إضافة خمسة حواسيب بشكلٍ يومي، من يوم الاثنين إلى يوم الخميس. كم عدد الحواسيب الموجودة في الغرفة الآن؟
الحل بلغة بايثون:
"""كان هناك تسعة حواسيب في الغرفة. تم إضافة خمسة حواسيب بشكلٍِ يوميّ، من يوم الاثنين إلى يوم الخميس. كم عدد الحواسيب الموجودة في الغرفة الآن؟"""
computers_initial = 9
computers_per_day = 5
num_days = 4  # 4 days between monday and thursday
computers_added = computers_per_day * num_days
computers_total = computers_initial + computers_added
result = computers_total
return result

سؤال: يملك شون خمس ألعاب. في عيد الميلاد حصل عل لعبتين إضافيتين من والدته ووالده. كم عدد الألعاب التي يملكها الآن؟
الحل بلغة بايثون:
"""يملك شون خمس ألعاب. في عيد الميلاد حصل عل لعبتين إضافيتين من كلٍّ من والدته ووالده. كم عدد الألعاب التي يملكها الآن؟"""
toys_initial = 5
mom_toys = 2
dad_toys = 2
total_received = mom_toys + dad_toys
total_toys = toys_initial + total_received
result = total_toys

سؤال: يملك جايسون 20 قطعة سكاكر. أعطى ديني بعضاً منها. يملك جايسون الآن 12 قطعة سكاكر. كم عدد قطع السكاكر التي أعطاها جايسون لديني؟
الحل بلغة بايثون:
"""يملك جايسون 20 قطعة سكاكر. أعطى ديني بعضاً منها. يملك جايسون الآن 12 قطعة سكاكر. كم عدد قطع السكاكر التي أعطاها جايسون لديني؟"""
jason_lollipops_initial = 20
jason_lollipops_after = 12
denny_lollipops = jason_lollipops_initial - jason_lollipops_after
result = denny_lollipops
```
بالإنجليزية:
```
MATH_PROMPT = '''
Q: There were nine computers in the server room. Five more computers were installed each day, from monday to thursday. How many computers are now in the server room?

# solution in Python:
"""There were nine computers in the server room. Five more computers were installed each day, from monday to thursday. How many computers are now in the server room?"""
computers_initial = 9
computers_per_day = 5
num_days = 4  # 4 days between monday and thursday
computers_added = computers_per_day * num_days
computers_total = computers_initial + computers_added
result = computers_total
return result


Q: Shawn has five toys. For Christmas, he got two toys each from his mom and dad. How many toys does he have now?

# solution in Python:
"""Shawn has five toys. For Christmas, he got two toys each from his mom and dad. How many toys does he have now?"""
toys_initial = 5
mom_toys = 2
dad_toys = 2
total_received = mom_toys + dad_toys
total_toys = toys_initial + total_received
result = total_toys


Q: Jason had 20 lollipops. He gave Denny some lollipops. Now Jason has 12 lollipops. How many lollipops did Jason give to Denny?

# solution in Python:
"""Jason had 20 lollipops. He gave Denny some lollipops. Now Jason has 12 lollipops. How many lollipops did Jason give to Denny?"""
jason_lollipops_initial = 20
jason_lollipops_after = 12
denny_lollipops = jason_lollipops_initial - jason_lollipops_after
result = denny_lollipops

Q: {question}

# solution in Python:
'''

```
 الآن يمكننا أن نمرّر التعليمة الكاملة إلى GPT-3
```
llm_out = llm(MATH_PROMPT.format(question=question))
print(llm_out)
```
ويكون الخرج:
```
استغرقت رحلة إيما بالطائرة إلى سياتل مدة 60 دقيقة. ثم استغرقت رحلتها بالقطار مدة ساعتين إلى بورتلاند، ثم رحلة بالحافلة لمدة 30 دقيقة إلى فانكوفر. كم استغرقت من الوقت للوصول إلى فانكوفر؟
plane_ride = 60
train_ride = 2 * 60  # 2 hours in minutes
bus_ride = 30
total_time = plane_ride + train_ride + bus_ride
result = total_time
```
بالإنجليزية:
```
Emma took a 60 minute plane ride to seattle. She then took a 2 hour train ride to portland, and then a 30 minute bus ride to vancouver. How long did it take her to get to vancouver?

plane_ride = 60
train_ride = 2 * 60  # 2 hours in minutes
bus_ride = 30
total_time = plane_ride + train_ride + bus_ride
result = total_time
```
أخيراً، يمكننا أن نمرّر التعليمة إلى التنفيذ بلغة بايثون للحصول على الإجابة:
```
exec(llm_out)
print(result)
```

الخرج يساوي 210، وهو صحيح.

راجع دفتر Jupyter لأجل هذا المثال [هنا](https://github.com/trigaten/Learn_Prompting/blob/main/docs/code_examples/PAL.ipynb).

## المزيد 

راجع أيضًا مثال [colab الخاص بـ PAL](https://colab.research.google.com/drive/1u4_RsdI0E79PCMDdcPiJUzYhdnjoXeXc?usp=sharing#scrollTo=Ba0ycacK4i1V).


