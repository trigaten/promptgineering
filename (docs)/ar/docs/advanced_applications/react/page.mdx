export const metadata = {
  sidebar_position: 40,
  title: "🟢 نموذج لغات كبيرة يفكّر ويتصرّف",
};

# نموذج لغات كبيرة يفكّر ويتصرّف

نموذج ردّ الفعل ReAct (التفكير المنطقي، التصرف) هو صيغةٌ لتفعيل نموذج لغاتٍ يحلّ المهام المعقّدة باستخدام اللغات المنطقية الطبيعيّة. تم تصميم ردّ الفعل لأجل المهام التي تسمح لنماذج اللغات الكبيرة بأداء مجموعةٍ من التصرفات. كمثال، كما في نظام MRKL ، يمكن لنماذج اللغات الكبيرة التفاعل مع واجهات تطبيقات خارجيّة لاستيراد المعلومات. وعندما يتمّ سؤالها ستتمكّن من الاختيار والتصرّف لاستيراد المعلومات، ثمّ الإجابة على السؤال اعتماداً عليها.

يمكن أن نعتمد على أنظمة ReAct بشكلٍ مشابهٍ لأنظمة MRKL، ومع إضافة القدرة على التفكير بالفعل الذي يجب أن تقوم به.

تمعّن بالصورة التالية. السؤال الموجود في المربّع العلوي مصدره HotPotQA2، وهو سؤال يجيب على مجموعة بيانات يتطلب تفكيرًا معقدًا. يمكن لنموذج ReAct الإجابة على السؤال من خلال التفكير أولاً في السؤال (الفكرة 1)، ثم تنفيذ إجراء (الفصل 1) لإرسال استعلام إلى Google. ثم يتلقى ملاحظة (Obs 1)، ويستمر في حلقة التفكير والفعل والملاحظة حتى يصل إلى نتيجة (الفصل 3).
![ReAct System](https://learnprompting.org/assets/images/react_qa-c10be33ab46c41e5bf4dee9066df3e41.webp)

قد يتعرّف القراء الذين يملكون معرفة بالتعلّم المعزز على هذه العملية المشابهة للحلقة الواقعيّة الكلاسيكية للحالة، والإجراء، والمكافأة، والحالة،... توفر أنظمة ردّ الفعل بعض الصيغ الرسمية لهذا في ورقتهم البحثية.

## النتائج

استخدمت Google برنامج PaLM3 في تجاربها مع نماذج ReAct. تُظهر المقارنات مع التعليمات القياسية (السؤال فقط) وسلسلة الأفكار والتكوينات الأخرى أن أداء نموذج ReAct واعد لمهام الاستدلال المعقدة. كما تجري Google دراساتٍ على مجموعة بيانات FEVER4، والتي تغطي استخلاص الحقائق والتحقق منها.

![ReAct Results](https://learnprompting.org/assets/images/react_performance-80d12fa8026a0599d05fd2dabcc8a283.webp)
