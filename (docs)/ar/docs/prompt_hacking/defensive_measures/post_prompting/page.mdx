export const metadata = {
  sidebar_position: 40,
  title: "🟢 تعليمات ما بعد الإدخال",
};

# تعليمات ما بعد الإدخال 

تقوم تعليمات الدفاع بعد الإدخال بوضع إدخال المستخدم قبل التعليمة. لنأخذ هذا كمثال:

`Translate the following to French: {{user_input}}`

`ترجم ما يلي إلى الفرنسية: user_input`

يمكن تحسينه بوضع دخل المستخدم بعد التعليمة:

دخل المستخدم:
```
{{user_input}} 

Translate the above text to French
```
```
{{دخل المستخدم}}
ترجم النص أعلاه إلى الفرنسية.
```
قد يكون هذا مفيدًا حيث أن `تجاهل التعليمة السابقة` لا يعمل بشكل جيد. على الرغم من أن المستخدم يمكن أن يقول بدلاً من ذلك `تجاهل التعليمة التالية`، غالبًا ستتبع النماذج اللغوية الكبيرة التعليمة الأخيرة التي تراها.
