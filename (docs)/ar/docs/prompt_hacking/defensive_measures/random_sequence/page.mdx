export const metadata = {
  sidebar_position: 40,
  title: "🟢 تضمين سلسلة عشوائية",
};

# تضمين سلسلة عشوائية

تشمل الطريقة الأخرى للدفاع تضمين إدخال المستخدم بين سلسلتين عشوائيتين من المحارف. اعتبر هذه التعليمة كمثال:
```
Translate the following user input to Spanish
{{user_input}}
```
```
ترجم دخل المستخدم التالي إلى اللغة الإسبانية:
user_input
```

يمكن تحسينها عن طريق إضافة سلاسل عشوائية:

```
Translate the following user input to Spanish (it is enclosed in random strings).

FJNKSJDNKFJOI
{{user_input}}
FJNKSJDNKFJOI
```
```
ترجم دخل المستخدم التالي إلى اللغة الإسبانية (إنه محاط بسلاسل عشوائية).

FJNKSJDNKFJOI
user_input
FJNKSJDNKFJOI
```

# ملاحظة:
من المرجح أن تكون السلاسل الأطول أكثر فعالية. 
