export const metadata = {
  sidebar_position: 40,
  title: "🟢 معايرة نماذج اللغات الكبيرة",
};

# معايرة نماذج اللغات الكبيرة

من الممكن مواجهة بعض التحيزات التي تظهرها نماذج اللغات الكبيرة عبر معايرة **توزيع المخرجات**.

**ما معنى معايرة نماذج اللغات الكبيرة تحديداً؟**

لنتفحّص مجموعةً من الأمثلة السريعة: لنقل أننا نملك مهمّةً لتحديد المشاعر بتسميتين مختلفتين، `إيجابي` أو `سلبيّ`. لنضع في عين الاعتبار ما يحدث عندما يُطلب من نماذج اللغات الكبيرة `دخل: لا شيء: الشعور:`. لا يحتوي هذا الدخل على أي *سياق* يمكن للنموذج استخدامه للتنبؤ بالمشاعر، لذلك يطلق عليه `دخلٌ من السياق`.

وبما أنّ `لا شيء` ليس مفهوماً إيجابياً أو سلبياً، قد نتوقع من النموذج أن احتمالاً يساوي 0.5 لكلٍّ من التسميتين `سلبي` و `إيجابي`. وبكل الأحوال، غالباً (ولهذا المثال) لن يكون الحال مماثلاً.

```
p("Positive" | "Input: nothing Sentiment:") = 0.9

p("Negative" | "Input: nothing Sentiment:") = 0.1
```

بالنظر إلى احتمالات تسمية هذه المدخلات الخالية من السياق، فإننا نعلم أن توزيع مخرجات النموذج من المحتمل أن يكون متحيزًا نحو التسمية الإيجابية. وقد يتسبب هذا في تفضيل النموذج للـ `إيجابي` لجميع المدخلات، حتى لو لم تكن المدخلات `إيجابي` بالفعل.

إذا تمكنا بطريقة أو بأخرى من معايرة توزيع المخرجات، بحيث يتم تعيين احتمالية قدرها 0.5 للمدخلات الخالية من السياق لكل من `إيجابي` و`سلبي`، فيمكننا في كثير من الأحيان إزالة الانحياز نحو `إيجابي` وسيكون النموذج أكثر موثوقية في كل من المدخلات الخالية من السياق و المدخلات مع السياق.

## الحل غير التقني

يتمثل الحل غير التقنيّ لهذه المشكلة في تقديم عدد قليل من الأمثلة التي يتم فيها تعيين نماذج خالية من السياق بشكل فعّال باحتمالية قدرها 0.5 لكل من `إيجابي` و `سلبي`.

على سبيل المثال، يمكننا تقديم الأمثلة القليلة التالية التي توضح تصنيف كل نموذج خالٍ من السياق على أنه `إيجابي` و `سلبي`:

```
الدخل: أنا أكره هذا الفيلم. المشاعر: سلبية
الدخل: أنا أحب هذا الفيلم. المشاعر: إيجابية
الدخل: غير متوفر المشاعر: إيجابي
الدخل: غير متوفر المشاعر: سلبي
الدخل: لا شيء الشعور: إيجابي
الدخل: لا شيء الشعور: سلبي
الدخل: أنا أحب البيض. المشاعر:
```
بالإنجليزية:
```
Input: I hate this movie. Sentiment: Negative
Input: I love this movie. Sentiment: Positive
Input: N/A Sentiment: Positive
Input: N/A Sentiment: Negative
Input: nothing Sentiment: Positive
Input: nothing Sentiment: Negative
Input: I like eggs. Sentiment:
```
بحسب معرفتي،  لم يتم استكشاف هذا الحل في البحث، ولست متأكدًا من مدى نجاحه في الممارسة العملية. ومع ذلك، فهو حلٌّ بسيط يوضّح ما يحاول المعايرة تحقيقه.

## الحلّ التقنيّ

هناك حل آخر لذلك وهو **المعايرة السياقي**، حيث نقوم بضبط معالم المعايرة الخاص، والتي تضمن أن المدخلات الخالية من السياق مثل `:Input: nothing: Sentiment` تحصل على احتماليّة بحوالي 0.5 لكلا التصنيفين. لاحظ أنه من الناحية العملية، تقوم هذه الطريقة بإجراء المعايرة على عدة مدخلات مختلفة خالية من السياق (على سبيل المثال؛ `Input: N/A Sentiment: , Input: [MASK] Sentiment:`). إذ يحسب متوسط ​​معالم المعايرة التي تعمل بشكل أفضل لكل المدخلات الخالية من السياق للعثور على أفضل معالم المعايرة لنماذج اللغات الكبيرة.

### مثال

لنستعرض مثالاً لحساب معالم المعايرة لدخلٍ واحد خالٍ من السياق. لاحظ أن هذا المثال غير قابل للتكرار باستخدام GPT-3 لأنه لا يقتصر على التسميات `إيجابي` و `سلبي` وحسب.

فكّر مرة أخرى في المثال أعلاه حيث يقوم النموذج بتعيين الاحتمالات التالية للتسميات لدخلٍ خالٍ من السياق:
```
p("Positive" | "Input: nothing Sentiment:") = 0.9

p("Negative" | "Input: nothing Sentiment:") = 0.1
```

نريد أن نجد قيمة التوزيع الاحتمالي q مثل:
```
q("Positive" | "Input: nothing Sentiment:") = 0.5

q("Negative" | "Input: nothing Sentiment:") = 0.5
```
سنفعل ذلك عن طريق إنشاء تحويل خطي يضبط (يقوّم) احتمالات *p* 
*q^ =Softmax(W p^+b)*
تأخذ هذه المعادلة الاحتمالات الأصليّة *p^* ويطبّق الوزن *W*  والتحيّز *b*. ليكون التحيز والوزن معالم المعايرة، والتي عند تطبيقها على احتمالات المثال الخالي من السياق ستسفر عن نتيجة   [0.5,0.5]= *p*.

#### حساب W و b

سنحتاج إلى حساب الوزن والتحيّز. وهذه إحدى الطرق للقيام بذلك:
W=diag( p)^ −1
b=0
على الرغم من أنّ تعريف W قد يبدو غريبًا بعض الشيء في البداية، لكنه يأخذ فقط معكوس كل قيمة في *p* للعثور على W من شأنها أن تحول الاحتمالات الأصلية *p* في الاحتمالات المقوّمة [0.5، 0.5].

لنتحقق من أنّ هذا الأسلوب يعمل مع المثال أعلاه:

______________

كما ذكرنا أعلاه، سننفّذ هذه العملية نفسها للعديد من المدخلات المختلفة الخالية من السياق، ونحسب متوسط ​​معالم المعايرة الذي يعمل بشكل أفضل لكل مدخلات خالية من السياق للعثور على أفضل معالم المعايرة للنموذج. وهذا يعني أنه قد لا تعيّن معالم المعايرة النهائية أيًّا من المدخلات الخالية من السياق إلى [0.5، 0.5] بالضبط.

### طريقة أخرى
من الممكن ضبط *b* إلى _*p*، و*W* إلى مصفوفة الهوية. تعمل هذه الطريقة بشكل أفضل بكثير بمهام التوليد مقارنةً مع مهام التصنيف.

## ما تعلّمت

غالبًا ما تكون نماذج اللغات الكبيرة مهيئةً (متحيزةً) تجاه تسميات معينة. ويمكن استخدام المعايرة لمواجهة هذا التحيز.

